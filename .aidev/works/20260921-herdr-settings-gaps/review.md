# レビュー: このブラウザの設定を増やす（サイドバーの幅・状態の記号・scrollback）

## タスク点検ログ（coding 工程内・「3.3」(b)）

`mode: autonomous` なので、自前の差分を生むタスクはすべて点検する（T12 は差分を持たないので対象外）。
指摘はその場で直した。書式は `- [重大度][条項] 箇所 指摘 / 対応`。

### T1（字形と読み上げの名前の表）

- [should][conv:-] `stateIndicator.test.ts` 「絵文字の範囲を使わない」テストが題名どおりのことを確かめていなかった
  （`cp < 0x1f000` だけを見ていて、BMP の絵文字 ✅ U+2705・✔ U+2714・⏳ U+23F3 を素通りする。点検者が `done` を ✅ にした
  写しで通ることを確かめた）/ 対応: `/\p{Emoji}/u` で属性を見る形に直し、`done` を ✅ にすると落ちることを負の対照で確かめた
- [should][conv:-] 読み上げの名前の値を固定していたのが 2 つだけで、working と idle の名前を入れ替えても通った /
  対応: 5 つとも値を固定した

### T2（scrollback を決める純粋関数）

- [should][conv:-] `scrollback.test.ts` 「自動」のデスクトップ（サーバの上限をそのまま使う）を上限 5000 でしか見ておらず、
  `limit` を定数 5000 に替えても通った / 対応: 上限を 5000・10000・3000・0 で見る形に直し、定数に替えると落ちることを負の対照で確かめた
- [should][conv:-] 保存値が 0 のときに選択肢へ足すことを見ておらず、`saved && …`（0 を偽とみなす形）に替えても通った。
  そうなると上限 0 で選んだ 0 を上限 5000 で開いたとき「どの行も選ばれていない」になる / 対応: `scrollbackChoices(5000, 0)` を足し、
  `saved && …` に替えると落ちることを負の対照で確かめた

### T3（サイドバーの幅と折りたたみの保存）

- [should][conv:-] `ActionDispatcher.test.ts` の `beforeEach` が `sessionStorage` しか消しておらず、`toggleSidebar` が
  `localStorage` に書くようになったので、畳んだ状態が同じファイルの後続のテストへ残った（`--repeats 1` で 2 回目が落ちる）/
  対応: `localStorage.clear()` を足し、理由をコメントに書いた。`--repeats 1` で通ることを確かめた
- [nit][conv:-] 「範囲の外は丸めずに既定へ」をストアの側で見ておらず、読み戻しで丸める作りになっても通った /
  対応: 保存値 9999 が 240 で起動することを見るテストを足した
- [nit][conv:-] `view.ts` の新しいコメントの AC 番号に work 名が無く、同じファイルにある別 work の AC と見分けにくかった /
  対応: `20260921-herdr-settings-gaps の AC1` の形にそろえた

### T4（記号表示と scrollback の設定のストア）

- [should][conv:-] 「反映と保存を同時に」の反映の側を見ておらず、`statusSymbols.value = v;` を消しても通った
  （押しても再読み込みまで記号が変わらない壊れ方）/ 対応: 同じストアの値が変わったことを見る行を足した
- [nit][conv:-] 「\"auto\" が保存される」と言いながら読み戻しだけを見ていた / 対応: `readPrefs()` に `"auto"` が書かれたことも見る

### T8（action・文脈・入口の改名）

- [should][conv:regression-negative-control] モバイルのボタンのテストが文字だけを見ていて、`aria-label="通知の設定"` が残っても
  通った（そのとき読み上げは aria-label が勝つ）。コメントは確かめていないこと（読み上げでも「設定」）を書いていた /
  対応: `aria-label`・`aria-labelledby` が無いことも見る形にした
- [nit][conv:-] `keymap.ts` の `s` の上のコメントと `KeyRouter.test.ts` のコメントが改名に追いついていなかった
  （「後で外観と設定が来たら育てればよい」と、いま起きていることを先の話として書いていた）/ 対応: 経緯の書き方をそろえた
- （実装者の自己点検）ヘルプの `s` の表記を `toContain("設定")` で見る形にしていたが、同じ画面の「未対応（後続: 外観と設定）」に
  含まれるので**何を書いても通る**。`dt` で行を特定する形に直し、表記を「通知」にすると落ちることを負の対照で確かめた

### T5（状態の点を `StateIcon` に寄せる）

- [should][conv:-] 記号「入」のとき、エージェントの居る行（1em の箱）と居ない行（0.6em の丸）で印の幅が違い、ラベルの位置が
  そろわなかった（点検者が Chromium で測り、ラベルの左端が約 5.6px ずれた。畳んだサイドバーでは行の高さが変わる）/
  対応: 印は記号の入／切・エージェントの有無にかかわらず**同じ 1em の箱**を占め、丸は `::before` で箱の中に以前と同じ 0.6em で描く。
  サイドバーの 2 行目の字下げを箱に合わせ、その行の `font-size: 0.85em` で割り戻した（以前の 1.1em はこれを忘れて 2.3px ずれていた）
- [should][conv:-] 3 か所を `StateIcon` に置き換えたことを守るテストが無かった（`data-state` だけを見ていて、素の `<span>` に
  戻しても通る。戻すと呼ぶ側の CSS が無いので点すら描かれない）/ 対応: サイドバー・goto・モバイルのピッカーそれぞれに、
  印が字形と `role="img"`・`aria-label` を持つことを見るテストを足した
- [nit][conv:-] 記号「切」のテストが `role` と `title` を見ていなかった（役割の無い span に名前を付けることは ARIA 1.2 で禁止）/
  対応: 両方見る

### T6（サイドバーの幅のドラッグと保存）

- [should][conv:-] **畳んでいる間も境目のドラッグが効き、見えていない幅が保存された**（点検者が写しで確かめた：畳んだまま動かすと
  保存値が 340 になり、展開すると一度も見ていない幅で開く）。保存するようになったことで表に出た / 対応: 畳んでいる間は
  `pointerdown` で何もしない。テストを足し、ガードを外すと落ちることを負の対照で確かめた
- [should][conv:-] ドラッグの起点がストアの幅であることを守っておらず、起点を既定に固定する・累積で足す変異が通った /
  対応: 保存値 280 から 2 回動かすテストを足し、起点を既定に固定すると落ちることを負の対照で確かめた
- [nit][conv:-] ドラッグしていないときにダイアログが開いても保存しないことを見ていなかった / 対応: テストを足した

### T7（scrollback の結線）

- [should][conv:-] `main.ts` で `settings` のストアを既存の JSDoc と `getScrollbackLines` の間に差し込んだため、JSDoc が
  `settings` に付き、しかも本文が設定を足す前の規則のままだった / 対応: ストアを他のストアと同じ所で作り、JSDoc を
  「設定 → 端末の種類 → サーバの上限」の順と D6 に書き直した
- [nit][conv:-] `ViewSyncOptions` の注記が「端末が無いときの予備」と書いていたが、`registry.get` が `undefined` になる経路は無く、
  予備が効くのは「作ったときの値が無い（`getScrollbackLines` を渡していない）」とき / 対応: 注記を直した
- [nit][conv:regression-negative-control] 行数 0 のケースが無く、`??` を `||` に替えても通った（0 で作った端末に 5000 を求める＝
  D6 が防ぐ食い違い）/ 対応: 0 の端末を足し、`||` に替えると落ちることを負の対照で確かめた
- [nit][conv:-] 端末の種類の型を直書きしていた（`net/ports.ts` に `ClientKind` がある）/ 対応: `DeviceKindKey` は `ClientKind` を使い、
  `main.ts` の余分な注釈を外した

### T9（設定ダイアログを 3 節に）

- [should][conv:regression-negative-control] 端末の節のテストが**取り付けた後の変化**を見ておらず、上限を取り付けた時点の値に写す・
  選ばれている行を写す変異が通った。実物の App は起動時にダイアログを取り付け（`App.vue`）、本当の上限は後から snapshot で届くので、
  **テストしていない道が本番の道**だった。また「行を選ぶと、その行が選ばれる」はテスト自身が `.checked` を入れてから読んでいた /
  対応: 取り付けた後に上限が届くと追従するテストと、設定の値が外から変わると選ばれている行が移るテストを足し、`.checked` を
  テスト側で入れない形にした。上限を写す変異・`:checked` を外す変異で落ちることを負の対照で確かめた
- [nit][conv:-] 通知の節の `<ul>` が `<section>` の中へ字下げされていなかった / 対応: 直した
- [nit][conv:-] 題名を `<h2>` にしたが大きさを指定しておらず、UA の既定で以前より 5 割大きくなっていた / 対応: 以前と同じ 1em を明示
- [nit][conv:-] テスト名に余計な空白 / 対応: 直した

### T10（文書）

- [should][conv:-] 対応表の「対応 AC」欄で AC3（3 種すべての壊れた値）の扱いが行ごとに違った / 対応: H06・H23b にも AC3 を入れた
- [should][conv:-] `docs/verification.md` の手順が、今も「`s` 等は未対応（後続: …）と出る」と書いていた（前 work から `s` は
  通知の設定、今は設定を開く）/ 対応: まだ未対応の `shift+r` を例にし、`s` は設定を開くと書いた
- [nit][conv:-] モバイルの上のバーのボタン一覧に「設定」が無かった / 対応: 足した
- [nit][conv:-] scrollback の手順が、ブラウザの設定が「自動」のときにしか成り立たないことを書いていなかった / 対応: 条件を書いた
- [nit][conv:-] `--scrollback` を「上限」と書いたが、モバイルの「自動」（1,000）は押さえない / 対応: 「数を選んだときだけ押さえる」と書き直した
- [nit][conv:-] ボタン名を「［設定］」と書いていた（docs の既存の書き方は「」）/ 対応: そろえた
- [nit][conv:-] H23b の製品側の注記を「herdr の機能」欄に入れていた / 対応: 既存の行と同じく分類欄の括弧に移した

### T11（E2E）

- [should][conv:-] AC9「その後に開く pane から効く」を確かめていなかった。選んでから新しい pane を開く間に「開き直し」を挟んでいたので、
  既存の pane も新しい pane も同じ行数を求め、「設定を起動時に 1 回だけ読む」形に壊しても通った / 対応: 同じページのまま、
  選ぶ前の pane（自動の 5,000 行）と、選んだ後に開いた pane（1,000 行）を pane の id で分けて見る形にした
- [should][conv:-] AC15 が前提（このブラウザの設定が実際に変わったこと）を確かめていなかった。操作が効かなくても既定のまま通る /
  対応: 別の context を開く前に、1 つ目の画面が 300px・記号「切」・「1,000 行」であることを見る
- [should][conv:-] AC3 が、仕込んだ壊れた値がページに届いたことを確かめていなかった（`origin` がずれると保存域は空で、既定の判定が
  仕込みと無関係に通る）/ 対応: 前提として `localStorage` の値を確かめ、同じ経路で正しい値を仕込むとその値で開く対照を足した
- [nit][conv:-] AC3 の範囲外の `statusSymbols: "off"` は真の文字列で、検証を通さない退行（`?? true`）を見逃す / 対応: 0 にした
- [nit][conv:-] AC-I3 のフォーカスの判定が間接的で、末尾の文言が言い過ぎていた / 対応: `toBeFocused()` で直接見て、文言を直した
- [nit][conv:e2e-observe-browser] `watchPaneSubscribes` の注記と既存の `watchClientViews` の注記（`Network.enable` より前に張られた
  接続は見えない）が観測と食い違っていた / 対応: 「`Network.enable` より前に送られたフレームが見えない」と正確に書き直した

### cross（タスクをまたぐ不変条件）

- [should][conv:-] 設定ダイアログの「表示」の節の注記が、字形 5 つと読み上げの名前 5 つを直書きしていた。T1 の `store/stateIndicator.ts` は
  「この表の唯一の置き場」と定め、design D4 は「字形を変えるときは 1 か所で差し替えられる」を前提にしていたのに、T9 が複製を
  持ち込んだ（表を変えるとサイドバー等は新しい字形、注記だけ古いまま）/ 対応: 注記を `DISPLAY_STATES` と `stateGlyph`/`stateLabel`
  から組み立てる形にした。表を差し替えると注記が追従することを見るテスト（`vi.mock` で表を差し替える）を足し、直書きに戻すと
  落ちることを負の対照で確かめた
- 散った規則のうち、scrollback の規則・`wtm.prefs.v1` のキーと所有・3 つの入口・幅の範囲と既定・テストの後始末には食い違いが無かった
  （点検者の確認。web の単体 924 件と typecheck も通過）

### 集計

点検したタスクは 11（T1〜T11。T12 は差分を持たないので対象外）。指摘は計 **38 件**（タスク 37・cross 1。うち must 0）。
**38 件のうち 27 件が「テストが守っていない／壊しても通る」系**で、直すたびに変異を入れて落ちることを確かめた（負の対照）。
実害のある不具合として表に出たのは 2 件——畳んだサイドバーの境目のドラッグで見えない幅が保存される（T6）、記号表示が「入」のとき
エージェントの居ない行だけ印の幅が違いラベルがずれる（T5）。

## レビュー ラウンド1

独立したコンテキストに、work 全体の差分（46 ファイル）を 5 観点（要件適合・価値適合・正確性・規約適合・保守性）でレビューさせた。
**タスク点検ログにある指摘は再掲させていない**。計 12 件（must 0 / should 5 / nit 7）。

- [should][conv:-] `SettingsDialog.vue:230` モバイル（特に iOS）には Esc キーが無く、閉じる手段は背景のタップだけ。3 節に広げてダイアログが
  高さ 512px になり（実測）、iPhone SE の幅では背景が上下 28px・左右 0px しか残らない。案内文も「Esc で閉じます」のまま /
  対応: 題名の行に［閉じる］を置き（確定ボタンではない。`HelpDialog` の［閉じる］が先例）、案内文を直す。狭い画面に収める（下の nit と合わせて）
- [should][conv:-] `SettingsDialog.vue:315` scrollback のラジオの行が高さ 16px・間隔 21px（iPhone 13 で実測）で、WCAG 2.5.8 の最小 24px 未満。
  押し間違えると隣の値がその場で保存される / 対応: 行に最小の高さを付ける
- [should][conv:-] `StateIcon.vue:59` 線の細い字形のコントラストが足りない。idle の ○（#6272a4）は背景 #282a36 で 3.03:1、hover の #343746 で 2.51:1、
  選ばれている行の #44475a で 1.94:1。blocked の × も #44475a で 2.91:1。WCAG 1.4.11 の 3:1 を下回り、以前の塗りの丸よりインクが少ないので
  「色に頼らず形で読める」効果が落ちる / 対応: idle と blocked の色を 3 つの背景すべてで 3:1 以上に上げ、字形を太字にする（decisions D6）
- [should][conv:regression-negative-control] `StateIcon.vue:49` タスク点検 T5 で直した不具合（印の幅がエージェントの有無で変わりラベルがずれる）と、
  design D2 の「呼ぶ側に CSS が残ると字形の後ろに丸が描かれる」に回帰を守るテストが無い（vitest は `<style>` を読まないので単体では原理的に見えない）/
  対応: E2E に 2 つ足し、1em の箱を外す変異で落ちることを確かめる
- [should][conv:-] `docs/verification.md:305` 実機の手順に、字形を目で確かめる項目が無い（D4 は「崩れる環境が見つかったら差し替える」としたのに、
  見つける手順がどこにも無い）/ 対応: 3 環境と実機の手順の期待に、字形の見え方と、開き直したときの幅と折りたたみを足す
- [nit][conv:-] `SettingsDialog.vue:236` `min-width: 22em` が content-box で、枠と padding を含めて 386px になり、幅 385px 以下の画面からはみ出す /
  対応: 画面の幅と高さに収める
- [nit][conv:-] `StateIcon.vue:85` unknown の `·` はインクが約 2px しかなく、さらに `opacity: 0.6` で、畳んだサイドバーではほぼ見えない /
  対応: opacity を外す（太字も効く）
- [nit][conv:-] `SettingsDialog.vue:126` 注記が字形の列と名前の列を別々に並べていて、対応を順番で数えるしかない（タッチでは `title` が出ない）/
  対応: 字形と名前を組にして並べる
- [nit][conv:-] `MobileShell.vue:87` 「設定」ボタンが 46×24px で、⌨（50×31px）より低い / 対応: 文字のボタンに最小の高さを付ける
- [nit][conv:-] `TerminalRegistry.ts:47` `getScrollbackLines` の注記が D6 より前の関係のまま / 対応: 書き直す
- [nit][conv:-] `stateIndicator.ts:13` `DISPLAY_STATES` の型では状態が増えても型で落ちず、並びも `STATE_PRIORITY` とコメントで合わせているだけ /
  対応: `STATE_PRIORITY` から導く
- [nit][conv:-] `settings.spec.ts:74` 入力待ちの偽エージェントの複製が 3 つ目（agent-detection・notifications に続く）/
  対応: **この work では直さない**。切り出すと安定している 2 本の spec を書き換えることになり、この work の範囲を越える。backlog へ送る

## 判定（ラウンド1）

should 5 件があるので coding へ差し戻す。

### T13（review ラウンド1 への対応。タスク点検ログ）

- [should][conv:regression-negative-control!] T13 で足した E2E の回帰テスト 2 つの負の対照の生の出力が `test-result.md` に貼られていなかった /
  対応: 「失敗の証跡」に T13 の節を足した（goto の分も含め 3 つ）
- [should][conv:-] ダイアログの `max-height: calc(100vh - 16px)` が UA の既定を上書きしていた。iOS Safari の `100vh` はツールバーを畳んだときの
  高さなので、ツールバーが出ている間は「閉じる」のある上端か下端が画面から切れうる（review が狙った iOS の小さい画面そのもの）/
  対応: `100%`（モーダルの `<dialog>` の包含ブロックは見えている領域）に替えた
- [nit][conv:-] ダイアログそのものがスクロールするので、小さい画面で端末の節まで下げると「閉じる」が流れる / 対応: 題名の行を sticky にした
- [nit][conv:-] 注記の区切り「・」（U+30FB）が状態不明の字形「·」（U+00B7）の前に来て、日本語フォントで同じ形に見える / 対応: 区切りを「、」に
- [nit][conv:regression-negative-control] design D2 の回帰を見る E2E がサイドバーだけで、goto に丸の CSS を戻しても通った / 対応: goto（`prefix+g`）でも
  背景が塗られないことを見る。goto に丸の CSS を戻すと落ちることを負の対照で確かめた
- [nit][conv:-] decisions D6 のコントラスト比がわずかに違っていた（WCAG 2.x の式で計算し直すと 3.36・3.32）、`test-result.md` の未検証の穴の
  1.4.11 の記述が古い / 対応: 計算し直した値に直し、穴の記述も直した
- [nit][conv:-] 実機の手順が「サイドバーで 5 つを形で見分けられる」ことを求めていたが、状態不明や完了はふだんの操作ですぐには出せない /
  対応: 5 つそろって見える場所として設定の「表示」の注記を案内した

## レビュー ラウンド2

同じく独立したコンテキストに、**ラウンド 1 の直しが新しい穴を作っていないか**を重点に見させた（差し戻しの 2 回目で最も起きやすい）。
ラウンド 1 の 12 件はすべて直っていることを確かめたうえで、計 3 件（must 0 / should 0 / nit 3）。nit だけだが、どれも小さく実害に近いので、
**承認の前に直した**。

- [nit][conv:-] `SettingsDialog.vue:262` **T13 で sticky にした題名の行に新しい穴が 2 つ**（ラウンド 1 の直しに由来）。(a) 題名の行がダイアログの
  padding の内側で止まり、その上の 16px の帯を中身が透けて流れる。(b) `scroll-padding` が無いので、Shift+Tab で上へ戻ると焦点の行が
  題名の行の下に隠れる（WCAG 2.4.11。レビュアーが再現ページで測った）/ 対応: 上の余白を題名の行に移し、`scroll-padding-top` を足した
- [nit][conv:regression-negative-control] `settings.spec.ts:240` design D2 の回帰の確認がサイドバーと goto だけで、3 か所目のモバイルのピッカーに
  丸の CSS を戻しても通った。居ない行の薄い丸（AC8「いまと同じ見え方」）も文字が空であることしか見ておらず、`::before` を消しても通った /
  対応: モバイルのピッカーで背景が塗られないことと、居ない行の `::before` が残ることを見る確認を足し、それぞれ戻して再ビルドすると
  落ちることを負の対照で確かめた
- [nit][conv:-] `docs/verification.md:308` 「注記とサイドバーの印は同じフォントで描かれる」と書いたが、T13 で印は太字・色付きになり、注記は
  小さく太字でも色付きでもない。並びも T13 で優先度の順（× ✓ ◐ ○ ·）に変わり、手順の並び（× ◐ ✓ ○ ·）と合わなかった /
  対応: 注記で見るもの（豆腐・絵文字にならないか）とサイドバーで見るもの（太さと色）を分けて書き、並びを優先度の順にそろえた

## 判定

**must・should は残っていない**。`aidev coverage` は gaps=0（tasks 承認時と同じ 21/21）。
work 全体の指摘は **must=0 / should=5 / nit=10**（ラウンド 1 が 0/5/7、ラウンド 2 が 0/0/3。タスク点検ログの件数は含めない）。
