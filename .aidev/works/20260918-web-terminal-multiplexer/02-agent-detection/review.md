# レビュー記録

## ラウンド1（2026-09-18T・review 工程・4本の独立レビュー委譲：herdr マニフェストエンジンの忠実さ／プロセス識別・プラットフォーム判定／状態追跡・監視ループ／組み込み配線と帰属表示）

- [must][conv:-] `ProcessMatcher.normalizedProcessName` が「`process.exe` が既に既知のエージェント名に一致する」枝（`if (lookupAgentKind(effective) !== null) return effective;`）で、`lookupAgentKind` 自身は basename 化して照合しているのに、返す候補文字列はフルパスのまま（basename 化していない）だった。これを受け取る `processPriority` の「自分自身の名前と一致するか」の比較（`ownName` は basename 化済み）が常に不一致になり、ラップされていない検出（本来 score=2）まで、ラップされた検出（score=3）と誤って同点にされる。同点は「先に見つかった方が勝つ」ため、`process.exe` がフルパス（`argv[0]` 由来。node_modules 配下のネイティブバイナリ等でよくある形）の候補が同じジョブに複数いるとき、結果が配列の順序に依存する誤検出になる（herdr は常に score=2/3 を正しく区別してから優先度で決める）。 — 根拠: packages/server/src/agent/ProcessMatcher.ts:55（旧）,75-80 / 対応: 修正済み（basename 化した文字列を返すように修正。配列の順序を入れ替えても結果が変わらないことを確かめる回帰テストを追加。`ProcessMatcher.test.ts`「配列の順序に依らない」）
- [should][conv:-] `skip_state_update=true` の検証（herdr は「`state` が明示的に `"unknown"` と書かれているか」を要求する）が、zod の `.default("unknown")` で `state` 省略時に早期に既定値が埋まってしまうため、`state` 自体を省略した `skip_state_update=true` のルールを誤って通してしまう（herdr は `rule.state != Some(Unknown)` で `None`（省略）も弾く）。 — 根拠: packages/server/src/agent/ManifestStore.ts:87（旧）,294 / 対応: 修正済み（`state` を `.optional()` にし、検証を通した後でコンパイル時に `?? "unknown"` で落とすように変更。省略時に無効・省略時に許容の両方の回帰テストを追加）
- [should][conv:-] 検証の上限のうち `MAX_RULES_PER_MANIFEST`（128）・`MAX_MATCHERS_PER_GATE`（32）と、`ManifestEngine` の同点優先度の並び順タイブレーク（D46 の「全ルール評価してから最高優先度を選ぶ」）、claude の `prompt_box_body`/`live_prompt_box`（末尾から2本目の水平線が上端）region が、実装は正しいが恒久的なテストで検証されていなかった。 — 根拠: packages/server/src/agent/ManifestStore.ts:169-174; packages/server/src/agent/ManifestEngine.ts:43 / 対応: 修正済み（4件とも回帰テストを追加。`ManifestStore.test.ts`・`ManifestEngine.test.ts`）
- [should][conv:-] `WindowsProcessInspector` の新規 `foregroundJob`（`deepestPathNodes`。フォーク・同着タイブレークの経路全体を返す）に、その経路そのものを検証するテストが無かった（既存の fork/tie-break テストは旧来の `foreground()`/`deepestNode` だけを経由し、`foregroundJob` 側は素通りしていた）。 — 根拠: packages/server/src/platform/WindowsProcessInspector.ts:92-100 / 対応: 修正済み（`foregroundJob` 経由で fork・同着タイブレークの両方を確認する回帰テストを追加。実装自体に誤りは無かった）
- [should][conv:-] `SessionService.updatePaneRuntime` が、`busy`/`title`/`cwd` は実際に値が変わったときだけ `pane.updated` を出すのに、`agent` だけは `patch.agent !== undefined` なら常に `pane.agent_status_changed` を出していた。`AgentTracker.update()` は herdr 由来のヒステリシス（D46・D50）の都合で、`visibleIdle`/`visibleBlocker`/`visibleWorking`（公開している `AgentInfo` には含まれない、判定内部だけのフラグ）だけが変わって `state` 等は同じ、という新しい `AgentInfo` オブジェクトを返すことがあり、そのたびに実質中身の変わらない再発行が全クライアントへ飛んでいた。 — 根拠: packages/server/src/session/SessionService.ts:326-328（旧）; packages/server/src/agent/AgentTracker.ts（`shouldPublish`） / 対応: 修正済み（`sameAgent` ヘルパーで `AgentInfo` の公開フィールドを比較してから発行するように変更。busy/title/cwd と同じ規約に揃えた。回帰テストを追加）
- [should][conv:-] `AgentMonitor.judge()` の `processInspector.foregroundJob()` 呼び出しに上限が無く、ネイティブ実装（`WindowsProcessInspector` が使う `@vscode/windows-process-tree` はコールバックベースで reject 経路を持たない）のコールバックが一度も呼ばれずに固まった場合、その pane の `schedule.inFlight` が永久に `true` のままになり、以後その pane は二度と判定されなくなる（クラッシュもログも無く、症状に気づきにくい）。 — 根拠: packages/server/src/agent/AgentMonitor.ts:153（旧） / 対応: 修正済み（`foregroundJobWithTimeout`（2秒。design の「状態反映2秒以内」の粒度に合わせた）を追加。タイムアウトすると通常のエラー経路と同じようにログへ残り、次の周期から判定を再開する。実際に固まる `ProcessInspector` を模した回帰テストを追加）
- [should][conv:-] `AgentMonitor.stop()` が同期的に `clearInterval`／購読解除だけ行って即座に返っていたため、`composeServer.close()` が `agentMonitor.stop()` の直後に `terminals.dispose(pane.id)`／`session` の後始末を行うと、実行中の判定周期（`/proc` 走査の await 中）がその後に完了し、破棄済みの pane を参照した状態のまま `session.updatePaneRuntime` を呼びうる（実害は小さいが未検証・未文書化のレースだった）。 — 根拠: packages/server/src/composeServer.ts:151（旧）; packages/server/src/agent/AgentMonitor.ts:80-87（旧） / 対応: 修正済み（`stop()` を `async` にし、実行中の周期（`inFlightJudgments`）を `Promise.allSettled` で待ってから返るように変更。`composeServer.close()` 側も `await` するように変更。stop() が実行中の周期を待つことを直接確認する回帰テストを追加）
- [nit][conv:-] `evidence/README.md` の「AgentMonitor の1周期の所要時間」の節が、実測を記録した decisions.md の決定番号を D51（`needsFastRecheck` の境界バグの修正）と誤って参照していた（正しくは D52：16 pane の実測）。 — 根拠: .aidev/works/20260918-web-terminal-multiplexer/evidence/README.md:43,60（旧） / 対応: 修正済み（D52 に訂正）
- [nit][conv:-] `RawManifestSchema` の `id`/`aliases` フィールドが、パースはされるが一切参照されない（herdr の `manifest_matches_agent`——`index.toml` の `path` が指すファイル自身の `id`/`aliases` と実際に一致するかの相互確認——に相当する処理が無い）。今回のポートには override/リモートマニフェストの仕組みが無い（意図的にスコープ外。D46）ため実害は小さいが、将来 `index.toml` の記述ミス（違うファイルを指す `path`）を検知できない。 — 根拠: packages/server/src/agent/ManifestStore.ts（`RawManifestSchema`） / 対応: 見送り（今回のスコープでは実害が無く、override 機構が無い前提と整合しない検証を足すと将来の設計変更時に足かせになる。override/リモートマニフェストを足す work があれば、そのときにこの検証も一緒に設計する）
- [nit][conv:-] `min_engine_version`（herdr は `Option<u32>`）・`priority`（herdr は `i32`）の zod スキーマが、整数・非負であることを検証していなかった（`min_engine_version = -1` や `priority = 1.5` を無検証で受理してしまう。実物のマニフェストには該当が無い）。 — 根拠: packages/server/src/agent/ManifestStore.ts:93,111（旧） / 対応: 修正済み（`.int()`／`min_engine_version` に `.nonnegative()` を追加）
- [nit][conv:-] `agents.ts` の `pathBasename` が、末尾が区切り文字のパス（`"foo/bar/"`）で herdr の `path_basename`（最後の**非空**要素を探す）と異なり空文字列を返していた（実際の実行ファイルパスが末尾スラッシュで終わることは無いため実害は無い）。 — 根拠: packages/server/src/agent/agents.ts:114-117（旧） / 対応: 修正済み（herdr と同じ「最後の非空要素」を探す実装に変更。回帰テストを追加）
- [nit][conv:-] `AgentMonitor.judge()` が、起動猶予（3秒）の間の高速再確認（100ms間隔）のたびに `ManifestEngine.evaluate()` を無条件に呼んでおり、`AgentTracker.update()` 側で結果を捨てているだけだった。D46 の記述（「3秒間は `ManifestEngine.evaluate` を呼ばない」）とは字面上ずれるが、猶予中は `state` が `unknown` のまま変わらないという機能的な振る舞いは正しく、`evaluate()` 自体のコストも軽い（D52 の実測で支配的なのは `/proc` 走査側）。 — 根拠: packages/server/src/agent/AgentMonitor.ts:166-186 / 対応: 見送り（実害・実測上の影響が小さく、猶予中の判定を呼ばないようにするには `AgentTracker` に新しい公開 API（`needsFastRecheck` とは別に「純粋な起動猶予中か」を区別する手段）を足す必要があり、対効果が見合わない）

被覆（`aidev coverage`）は tasks 承認時と同じ gap 数のまま（新たな乖離なし）。全指摘を修正・確認した上で、typecheck／lint／`packages/protocol`+`packages/server` の自動テスト（344件）／`aidev smoke` を再実行し、全て通ることを確認した。

## ラウンド2（2026-09-18T・review 工程・ラウンド1の修正後の再点検）

ラウンド1の全指摘（must 1件・should 6件・nit 3件を修正、nit 2件は見送りと明記）が `review.md`「対応」欄・
`test-result.md`「ラウンド2」・`decisions.md` の各決定に記録されている状態を確認した。`aidev coverage --strict`
が tasks 承認時と同じ gap 数（`struct=0`・`cover=9`。いずれも他 subtask 未着手による非致命の gap）のままで、
この修正ラウンドによる新たな乖離が無いことを確認した。追加の指摘なし。
